---
let count = 0;
const setCount = () => {
    count += 1;
};

// Astro never runs client-side
if (typeof window !== "undefined") {
    setInterval(setCount, 1000);
}
---

<div>{count}</div>

<style>
    div {
        color: lightblue;
    }
</style>
