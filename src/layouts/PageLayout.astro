---
import Layout from "./Layout.astro";

export interface Props {
    title: string;
    desc: string;
}

const { title, desc } = Astro.props;
---

<Layout
    title={title}
    desc={desc}
>
    <slot />
</Layout>
