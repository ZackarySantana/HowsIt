---
import { Capitalize } from "@utils";
import Layout from "./Layout.astro";

const example = Astro.locals.example;
const desc = Astro.locals.desc;
---

<Layout
    title={"Example - " + Capitalize(example)}
    desc={desc}
    mainClasses="flex flex-col gap-4"
>
    <slot />
</Layout>
